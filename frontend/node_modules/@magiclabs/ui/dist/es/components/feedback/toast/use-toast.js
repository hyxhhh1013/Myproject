import { useContext, createContext } from 'react';
import { createErrorWithCode } from '../../../libs/exceptions.js';

const raiseToastError = (fn) => () => {
    throw createErrorWithCode('TOAST_PROVIDER_REQUIRED', 'useToast', `Before calling \`${fn}\`, wrap your application with \`<ToastProvider>\`.`);
};
const ToastContext = /* @__PURE__ */ createContext({
    createToast: /* @__PURE__ */ raiseToastError('createToast'),
    removeToast: /* @__PURE__ */ raiseToastError('removeToast'),
});
function useToast() {
    return useContext(ToastContext);
}

export { ToastContext, useToast };
//# sourceMappingURL=use-toast.js.map
