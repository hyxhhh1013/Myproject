import React from 'react';
import { createMedia } from '../../libs/theme/create-media.js';

const WrappedMediaContext = /* @__PURE__ */ createMedia();
const MediaProvider = (props) => {
    const { children } = props;
    return React.createElement(WrappedMediaContext.MediaContextProvider, null, children);
};
const Media = (props) => {
    return React.createElement(WrappedMediaContext.Media, Object.assign({}, props));
};

export { Media, MediaProvider };
//# sourceMappingURL=media-provider.js.map
