import React from 'react';
import { DEFAULT_THEME } from '../../libs/theme/default-theme.js';
import { getThemeVariables } from '../../libs/theme/theme-variables.js';

const ThemeHead = (props) => {
    const { theme = DEFAULT_THEME } = props;
    return (React.createElement("style", { id: "mg-theme-head", type: "text/css", 
        // eslint-disable-next-line react/no-danger
        dangerouslySetInnerHTML: { __html: getThemeVariables(theme).toCSS() } }));
};
ThemeHead.displayName = 'ThemeHead';

export { ThemeHead };
//# sourceMappingURL=theme-head.js.map
