import { createMedia as createMedia$1 } from '@artsy/fresnel';
import { BREAKPOINTS } from '../../constants.js';

/**
 * Wraps `createMedia` from `@artsy/fresnel`
 * with sensible defaults for Magic UI.
 */
function createMedia() {
    return createMedia$1({
        interactions: {
            landscape: 'not all and (orientation: landscape)',
            portrait: 'not all and (orientation: portrait)',
            hover: '(hover: hover)',
            notHover: '(hover: none)',
        },
        breakpoints: BREAKPOINTS,
    });
}

export { createMedia };
//# sourceMappingURL=create-media.js.map
