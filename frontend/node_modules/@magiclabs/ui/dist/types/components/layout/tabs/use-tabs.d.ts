import React from 'react';
import { MonochromeIconDefinition } from '../../icons';
interface TabConfig {
    id: React.Key;
    label: JSX.Element | React.ReactText;
    content: JSX.Element;
    icon?: MonochromeIconDefinition;
}
interface UseTabsConfig {
    id?: React.Key;
    label: string;
    size?: 'sm' | 'md' | 'lg';
    orientation?: 'horizontal' | 'vertical';
    tabs: TabConfig[];
}
export declare function useTabs(config: UseTabsConfig): {
    tablist: React.JSX.Element;
    tabpanel: React.JSX.Element[];
};
export {};
