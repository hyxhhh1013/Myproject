import React from 'react';
import { Theme } from '../../libs/theme/theme-types';
interface ThemePrerenderProps {
    theme?: Theme;
}
export declare const ThemeHead: React.FC<ThemePrerenderProps>;
export {};
