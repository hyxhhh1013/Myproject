import { usePrevious } from '../use-previous/index.js';

/**
 * Returns a `boolean` indicating whether the given `value` has changed since
 * the previous update.
 */
function useCompare(value) {
    var prevVal = usePrevious(value);
    return prevVal !== value;
}

export { useCompare };
//# sourceMappingURL=index.js.map
