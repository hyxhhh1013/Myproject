'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var _tslib = require('../../_virtual/_tslib.js');
var React = require('react');
var index$1 = require('../use-effect-trigger/index.js');
var index = require('../use-initial-render/index.js');

/**
 * Debounces an effect. If `delay` is zero,
 * the effect is executed synchronously.
 *
 * The effect executes synchronously on the first render.
 */
function useDebouncedEffect(effect, delay, deps) {
    if (delay === void 0) { delay = 0; }
    if (deps === void 0) { deps = []; }
    var isInitialRender = index.useInitialRender();
    var triggerEffect = index$1.useEffectTrigger(effect, deps);
    React.useEffect(function () {
        if (isInitialRender || !delay) {
            return effect();
        }
        var timeout = setTimeout(function () {
            triggerEffect();
        }, delay);
        return function () {
            clearTimeout(timeout);
        };
    }, _tslib.__spread([delay], deps));
}

exports.useDebouncedEffect = useDebouncedEffect;
//# sourceMappingURL=index.js.map
