'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var index$1 = require('../use-compare/index.js');
var index = require('../use-hash/index.js');

/**
 * Returns a `boolean` indicating whether the given `value` has changed since
 * the previous update based on a hash of its contents.
 */
function useHashCompare(value) {
    var hash = index.useHash(value);
    var didHashChange = index$1.useCompare(hash);
    return didHashChange;
}

exports.useHashCompare = useHashCompare;
//# sourceMappingURL=index.js.map
